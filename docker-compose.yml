version: "3.9"

services:
  paper-trader:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - /data/paper-trader:/data
    env_file:
      - .env
    environment:
      - DB_PATH=/data/paper_trader.db
    restart: unless-stopped
